$(document).ready(function() {
    $('#onepage').fullpage();
    // Options
var options = {
    offset: 1000
}

// Create a new instance of Headhesive and pass in some options
//var header = new Headhesive('.subMenu', options);
});